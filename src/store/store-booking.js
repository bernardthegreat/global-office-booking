const state = {
  apiUrl: process.env.API_URL
}

const mutations = {
}

const actions = {

}

const getters = {
  // checkAvailability (state) {
  //   const
  // }
}

export default {
  namespace: true,
  state,
  mutations,
  actions,
  getters
}
